# 使用Amazon Corretto官方镜像（AWS维护的OpenJDK发行版）
FROM amazoncorretto:17-alpine

# 设置时区和编码
ENV TZ=Asia/Shanghai
ENV LANG=C.UTF-8

# 安装curl
RUN apk add --no-cache curl

# 设置工作目录
WORKDIR /app

# 复制JAR文件
COPY target/*.jar app.jar

# 暴露端口
EXPOSE 8080

# 启动应用
ENTRYPOINT ["java", "-jar", "app.jar"] 